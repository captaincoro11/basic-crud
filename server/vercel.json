{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  // Specify your entry point file here
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"  // Map all incoming requests to the entry point
    }
  ]
}
